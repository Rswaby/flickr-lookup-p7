import React from "react";

const Loader = () => {
  // A simple spinner component that will display while fetching data.
  return (
    <div className="loader">Loading...</div>
  );
}

export default Loader;
